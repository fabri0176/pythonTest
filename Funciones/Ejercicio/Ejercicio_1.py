def area_rectangulo(base,altura):
    area = base * altura
    return area

base = 15
altura = 10
area = area_rectangulo(10,5)

print("El area del rectangulo base = "+str(base)+", altura = "+str(altura) + ", area = "+str(area))
